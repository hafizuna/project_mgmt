# Environment configuration for external network access
# Use this when testing with other computers on the same network

# Backend API URL - use your computer's IP address
VITE_API_URL=http://100.115.92.202:4000/api

# Optional: Jitsi domain (uses public server by default)
VITE_JITSI_DOMAIN=meet.jit.si